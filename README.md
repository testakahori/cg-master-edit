# CG-Master Edit

A browser-based CG (Comic/Manga) image editor built with React and Vite.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Open `http://localhost:3000` in your browser.

## Build

```
npm run build
```
